import React from 'react';
import Footer from '../Footer';

const Tests = () => {
    return (
        <div className="fade-in">
            <Footer />
        </div>
    );
};

export default Tests;
