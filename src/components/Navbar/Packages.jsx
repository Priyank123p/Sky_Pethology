import React from 'react';
import Footer from '../Footer';

const Packages = () => {
    return (
        <div className="fade-in">
            
            <Footer />
        </div>
    );
};

export default Packages;
